[buildout]
parts = 
    django
    omelette

find-links = 
    http://code.codesyntax.com/static

\# Add here any other egg you want to depend on
eggs =
   
\# Add here any other egg you are developing now
develop-eggs = 
    \#src/my-small-egg

[django]
recipe = djangorecipe
version = ${django_version}
project = ${project}
settings = settings
eggs = 
   \${buildout:eggs}

[omelette]
recipe = collective.recipe.omelette
eggs = \${django:eggs}
packages = \${django:location}/django ./django